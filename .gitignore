node_modules/
dist/
build/
coverage/
.turbo/
.cache/
*.tsbuildinfo

# models / artifacts (large, regenerable)
models/
*.onnx
*.onnx_data*
*.mlpackage/
*.gguf
*.safetensors
*.sha256
*.sig

# python
__pycache__/
*.py[cod]
*$py.class
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# env / secrets
.env
.env.*

# runpod transient state
.runpod-state

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# logs
*.log
npm-debug.log*

# tmp
tmp/

# eval — internal-only state. `private/` holds training scripts +
# runpod orchestration (not published — recipe lives in
# docs/v10/V10_JOURNAL.md). `results/` holds bench artifacts (large,
# regenerable). Bench scripts under `packages/eval/scripts/` ARE
# tracked for reproducibility.
packages/eval/results/
packages/eval/private/
packages/eval/tests/

# sub-package lockfiles leak dev registry URLs; root lockfile is canonical
packages/*/package-lock.json

# Claude Code session state
.claude/

# generated SBOM
bom.json
bom.xml

# generated test artifacts
.nullpii-test-artifacts/
