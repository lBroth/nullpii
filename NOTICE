nullpii
Copyright 2026 nullpii contributors

This product includes software developed by the nullpii contributors.

Licensed under the Apache License, Version 2.0 (see LICENSE).

------------------------------------------------------------------------
Third-party attributions
------------------------------------------------------------------------

This product depends on the following third-party components.
Run `npm run license-check` for the full machine-readable list.

Runtime (npm):
  - @anush008/tokenizers       MIT       (NAPI bindings to HF tokenizers)
  - debug                       MIT
  - onnxruntime-node            MIT       (peer, optional)
  - commander                   MIT       (CLI)
  - chalk                       MIT       (CLI)
  - cli-progress                MIT       (CLI)

Build & dev:
  - typescript                  Apache-2.0
  - vitest                      MIT
  - @vitest/coverage-v8         MIT
  - @biomejs/biome              MIT or Apache-2.0
  - madge                       MIT
  - license-checker             BSD-3-Clause
  - lefthook                    MIT
  - tsx                         MIT

Research kit (`packages/eval/`, Python — gitignored, not published):
  - huggingface-hub             Apache-2.0
  - transformers                Apache-2.0
  - gliner                      Apache-2.0
  - torch                       BSD-3-Clause
  - onnx                        Apache-2.0
  - onnxruntime                 MIT
  - datasets                    Apache-2.0

Model artifacts (downloaded at runtime from HuggingFace Hub — NOT bundled
in the npm package; see README "First-call download"):

  Base / encoder (Apache-2.0):
    - urchade/gliner_multi_pii-v1 ........ GLiNER head on Microsoft
                                           mDeBERTa-v3 base — the backbone
                                           the per-domain LoRA adapters are
                                           merged into. (Zaratiana et al.,
                                           NAACL 2024.)
    - sentence-transformers/distiluse-base-multilingual-cased-v2 ........
                                           the router input encoder
                                           ("Google distiluse").

  Published weights:
    - lBroth/nullpii ..................... merged-LoRA ONNX shards +
                                           distiluse encoder + tokenizers +
                                           router prototypes (~6 GB FP32).
    - lBroth/nullpii-adapters ............ raw LoRA adapters (~17 MB),
                                           upstream of the merged repo.
    A model's licence can only be as permissive as the intersection of
    (a) the base model licence and (b) every dataset the fine-tune was
    trained on. The authoritative per-adapter training-data composition
    lives on the `lBroth/nullpii` HuggingFace model card — that card, not
    this file, is the source of truth for the weights' effective licence.
    See the BLOCKER note below: with the current training mix the weights
    CANNOT be released under a permissive (Apache-2.0 / MIT) licence.

  Fine-tune training datasets (per-domain LoRA adapters — devops / legal /
  medical / narrative / enterprise). Verified upstream licences (checked
  2026-05-11 against the HuggingFace dataset cards):
    - urchade/gliner_multi_pii-v1 ........ Apache-2.0  (base model)
    - sentence-transformers distiluse-... Apache-2.0  (router encoder)
    - NVIDIA Nemotron-PII (nvidia/Nemotron-PII) ... CC-BY-4.0 — commercial
                                           use allowed; attribution to
                                           NVIDIA required. Compatible
                                           with redistributing derived
                                           weights, attribution-conditioned.
    - Text Anonymization Benchmark (TAB / ECHR) train split ... MIT (TAB
                                           release); ECHR judgments are
                                           public court documents.
    - ai4privacy/pii-masking-300k ........ "other" — custom licence
                                           (`license.md`): academic use
                                           with citation OK; **commercial
                                           use gated** — must contact
                                           licensing@ai4privacy.com. NOT a
                                           permissive licence.
    - Isotonic/pii-masking-200k .......... cc-by-nc-4.0 — **NonCommercial**
                                           (one mirror tags it apache-2.0;
                                           upstream ai4privacy 200k is the
                                           same custom licence as the 300k).
                                           NOT a permissive licence.
    - project-authored synthetic corpora (Faker / templated; Apache-2.0,
      see packages/eval/datasets/) ......... Apache-2.0.

  *** BLOCKER — pre-publish (the npm PACKAGE is unaffected; this is about
  the model WEIGHTS only) ***
  The per-domain adapters are currently trained on ai4privacy/pii-masking-
  300k (custom, commercial-gated) and Isotonic/pii-masking-200k (CC-BY-NC).
  Weights derived from those cannot be redistributed under Apache-2.0 for
  commercial use. Before publishing the weights, pick one:
    (a) re-train the affected adapters on permissive-only data — Nemotron-
        PII (CC-BY-4.0) + TAB (MIT) + project-authored Faker/templated
        corpora (Apache-2.0) + any CC0/Apache datasets; OR
    (b) publish the weights under CC-BY-NC-4.0 (and drop the commercial
        framing); OR
    (c) obtain a commercial licence from ai4privacy.
  (The planned "drop Nemotron-PII from the enterprise adapter" sprint is a
  benchmarking-credibility move — Nemotron-PII's CC-BY-4.0 is NOT a licence
  problem — so it does not resolve this blocker.)

Bench / eval datasets (used only to measure F1 — never to train the
shipped weights): presidio-synthetic, ai4privacy-300k-heldout,
Isotonic-heldout, tab-echr test split, nemotron-pii-test. Each retains
its own licence; see packages/eval/datasets/README.md. (Eval-only use
does not affect the weights' licence, but note `*-heldout` slices come
from the same non-permissive ai4privacy / Isotonic sources above —
relevant if those eval JSONLs were ever to be redistributed.)

License posture:
  - 100% permissive runtime tree (MIT / Apache-2.0 / BSD / ISC / CC0).
    Zero LGPL, GPL, AGPL, or SSPL components reachable from the published
    npm package. Verified by `npm run license-check` in CI.
  - The npm package ships only TypeScript/JS source + compiled `dist/`.
    Model weights are fetched on first use from HuggingFace and are NOT
    part of the npm artifact; their licence is governed by the
    `lBroth/nullpii` model card (see above).
  - Research kit (packages/eval/, gitignored) is not published. Its
    Python deps include MPL-2.0 components (`certifi`, `tqdm` is
    dual-licensed MIT). MPL-2.0 is file-level copyleft and imposes
    no obligation on this project's own source.
