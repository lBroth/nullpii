nullpii
Copyright 2026 nullpii contributors

This product includes software developed by the nullpii contributors.

Licensed under the Apache License, Version 2.0 (see LICENSE).

------------------------------------------------------------------------
Third-party attributions
------------------------------------------------------------------------

This product depends on the following third-party components.
Run `npm run license-check` for the full machine-readable list.

Runtime (npm):
  - @anush008/tokenizers       MIT       (NAPI bindings to HF tokenizers)
  - debug                       MIT
  - onnxruntime-node            MIT       (peer, optional)
  - commander                   MIT       (CLI)
  - chalk                       MIT       (CLI)
  - cli-progress                MIT       (CLI)

Build & dev:
  - typescript                  Apache-2.0
  - vitest                      MIT
  - @vitest/coverage-v8         MIT
  - @biomejs/biome              MIT or Apache-2.0
  - madge                       MIT
  - license-checker             BSD-3-Clause
  - lefthook                    MIT
  - tsx                         MIT

Research kit (`packages/eval/`, Python — gitignored, not published):
  - huggingface-hub             Apache-2.0
  - transformers                Apache-2.0
  - gliner                      Apache-2.0
  - torch                       BSD-3-Clause
  - onnx                        Apache-2.0
  - onnxruntime                 MIT
  - datasets                    Apache-2.0

Model artifacts (downloaded at runtime from HuggingFace Hub — NOT bundled
in the npm package; see README "First-call download"):

  Base / encoder (Apache-2.0):
    - urchade/gliner_multi_pii-v1 ........ GLiNER head on Microsoft
                                           mDeBERTa-v3 base — the backbone
                                           the per-domain LoRA adapters are
                                           merged into. (Zaratiana et al.,
                                           NAACL 2024.)
    - sentence-transformers/distiluse-base-multilingual-cased-v2 ........
                                           the router input encoder
                                           ("Google distiluse").

  Published weights:
    - lBroth/nullpii ..................... merged-LoRA ONNX shards +
                                           distiluse encoder + tokenizers +
                                           router prototypes (~6 GB FP32).
    - lBroth/nullpii-adapters ............ raw LoRA adapters (~17 MB),
                                           upstream of the merged repo.
    These weights are published under Apache-2.0 by the nullpii
    contributors. A model's licence can only be as permissive as the
    intersection of (a) the base model licence and (b) every dataset the
    fine-tune was trained on. The authoritative per-adapter training-data
    composition and the resulting licence provenance live on the
    `lBroth/nullpii` HuggingFace model card — that card, not this file, is
    the source of truth for the weights.

  Fine-tune training datasets (per-domain LoRA adapters — devops / legal /
  medical / narrative / enterprise). Each retains its own upstream licence;
  see the respective HuggingFace dataset card and the `lBroth/nullpii`
  model card for which dataset feeds which adapter:
    - ai4privacy/pii-masking-300k
    - Isotonic/pii-masking-200k
    - the Text Anonymization Benchmark (TAB / ECHR) train split
    - NVIDIA Nemotron-PII train split
    - project-authored synthetic corpora (Faker / templated; Apache-2.0,
      see packages/eval/datasets/)

  Outstanding licence-audit item (pre-1.0): confirm that the
  ai4privacy-300k and NVIDIA Nemotron-PII training slices permit
  redistribution of derived weights under Apache-2.0; if either does not,
  re-train the affected adapter on a permissive corpus before publishing
  the weights. Eval-only datasets (presidio-synthetic, *-heldout) do not
  affect the weights' licence.

Bench / eval datasets (used only to measure F1 — never to train the
shipped weights): presidio-synthetic, ai4privacy-300k-heldout,
Isotonic-heldout, tab-echr test split, nemotron-pii-test. Each retains
its own licence; see packages/eval/datasets/README.md.

License posture:
  - 100% permissive runtime tree (MIT / Apache-2.0 / BSD / ISC / CC0).
    Zero LGPL, GPL, AGPL, or SSPL components reachable from the published
    npm package. Verified by `npm run license-check` in CI.
  - The npm package ships only TypeScript/JS source + compiled `dist/`.
    Model weights are fetched on first use from HuggingFace and are NOT
    part of the npm artifact; their licence is governed by the
    `lBroth/nullpii` model card (see above).
  - Research kit (packages/eval/, gitignored) is not published. Its
    Python deps include MPL-2.0 components (`certifi`, `tqdm` is
    dual-licensed MIT). MPL-2.0 is file-level copyleft and imposes
    no obligation on this project's own source.
